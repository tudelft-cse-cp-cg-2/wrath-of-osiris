image: maven:3-jdk-11

build:
        stage: build
        script:
                - cd client/
                - mvn clean compile
                - cd ../server/
                - mvn clean compile

test:
        stage: test
        script:
                - cd client/
                - mvn clean test
                - cd ../server/
                - mvn clean test
